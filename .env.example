# Safe local-development placeholders only. Copy to .env and replace locally.
POSTGRES_DB=applypilot
POSTGRES_USER=applypilot
POSTGRES_PASSWORD=replace-with-a-local-random-password
DATABASE_URL=postgresql+psycopg://applypilot:replace-with-a-local-random-password@postgres:5432/applypilot
APP_ENV=development
LOG_LEVEL=INFO
